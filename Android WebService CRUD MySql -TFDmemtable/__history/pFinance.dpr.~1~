program pFinance;

uses
  System.StartUpCopy,
  FMX.Forms,
  uMaster in 'uMaster.pas' {FrmMaster};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFrmMaster, FrmMaster);
  Application.Run;
end.
